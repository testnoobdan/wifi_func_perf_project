#!/usr/bin/env bash
set -euo pipefail
echo "Running pytest automation harness..."
export PYTHONPATH="$(pwd)/src:${PYTHONPATH:-}"
python -m pytest --maxfail=1 --disable-warnings -q --cov=src --cov-report=term-missing